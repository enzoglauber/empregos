/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdvertiseFooterService } from './advertise-footer.service';

describe('Service: AdvertiseFooter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdvertiseFooterService]
    });
  });

  it('should ...', inject([AdvertiseFooterService], (service: AdvertiseFooterService) => {
    expect(service).toBeTruthy();
  }));
});
